# Core library for communicating with the Interactive Brokers API
ib_insync

# Used to make HTTP requests to scrape messages from Discord
requests

# Securely loads environment variables from the .env file (for your auth token)
python-dotenv

# Enables colored logging output in the console for better readability
colorama

# A helpful library for parsing date and time strings from Discord
python-dateutil

